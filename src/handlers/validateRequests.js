const boom = require('@hapi/boom')

// TODO: Search how to validate with boom
function validateRequestHandler(schema, check) {
  return (req, res, next) => {}
}

module.exports = validateRequestHandler
